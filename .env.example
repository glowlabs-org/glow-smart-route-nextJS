MAINNET_RPC_URL="https://eth.merkle.io"
DEPLOYMENT_CHAIN="1"
DISCORD_GLOW_ALERT_WEBHOOK_URL=""
INTERNAL_SIM_URL="https://glow-mechanistic-twin-icrg-launch.vercel.app"
# ReplyCorp Connect on X widget brand identifier (public, safe to expose).
# Always "glow" (the ReplyCorp org slug) unless we're testing against a
# different brand on a staging environment.
NEXT_PUBLIC_REPLYCORP_BRAND_ID="glow"
