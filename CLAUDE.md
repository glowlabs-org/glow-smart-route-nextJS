# glow-smart-route-nextJS — Claude Code context

Frontend (Next.js 16, React 19, Tailwind 4, Wagmi 2 + Viem 2, Privy, Sentry; pnpm). Build uses
**webpack** (`next build --webpack`), not Turbopack.

This repo's agent guidance has a **single source: `AGENTS.md`** (shared by Claude Code, Codex, and
Cursor; it carries the frontend performance + design conventions). It is imported below, so Claude
Code loads it automatically when you work in this repo. Do not maintain a second divergent copy here.
Cross-repo overview, the live V2 system state, and file-location maps live in the workspace-root
`../CLAUDE.md` (auto-loaded).

@AGENTS.md
